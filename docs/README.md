# Agent docs

This directory is reserved for Markdown documents used during development, such as design specs, implementation plans, and other agent-facing notes.

The public static site for GitHub Pages is generated into [`html/`](../html), not `docs/`.
