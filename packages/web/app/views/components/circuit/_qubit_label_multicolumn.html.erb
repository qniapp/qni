<% label = local_assigns.fetch(:label) %>
<% row_span = local_assigns.fetch(:row_span) %>

<div class="row-span-<%= row_span %> flex items-center border-r-2 border-gray-400">
  <span class="font-semibold mr-2"><%= label %></span>
</div>
