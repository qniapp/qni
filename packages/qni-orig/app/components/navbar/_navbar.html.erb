<% navbar = local_assigns.fetch(:navbar) %>

<nav class="fixed top-0 left-0 right-0 z-20 bg-white shadow">
  <div class="max-w-7xl mx-auto px-4 lg:px-8">
    <div class="relative flex justify-between h-16">
      <div class="flex-1 flex items-stretch justify-start">
        <div class="flex-shrink-0 flex items-center">
          <%= component 'navbar/qni_logo' %>
          <%= component 'navbar/circuit_name', name: navbar.title if navbar.title %>
        </div>
      </div>
    </div>
  </div>
</nav>
