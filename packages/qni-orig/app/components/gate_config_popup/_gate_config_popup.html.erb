<% gate_config_popup = local_assigns.fetch(:gate_config_popup) %>

<div id="gate-popup--<%= gate_config_popup.type %>" class="gate-popup hidden">
  <div class="flex flex-col px-1 py-1">
    <label for="<%= gate_config_popup.type %>" class="block text-sm font-medium text-wolf"><%= gate_config_popup.label %></label>
    <div class="mt-1">
      <input class="gate-popup__input" type="text" name="<%= gate_config_popup.type %>">
      <div class="gate-popup__error-icon absolute inset-y-0 right-0 pr-6 flex items-center pointer-events-none">
        <%= exclamation_circle_icon %>
      </div>
    </div>
    <p class="gate-popup__input-error"><%= gate_config_popup.error_message %></p>
  </div>
</div>
