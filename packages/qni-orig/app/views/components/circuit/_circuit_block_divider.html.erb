<% body = yield.presence || raise('body is a mandatory') %>

<div class="flex-shrink-0 flex flex-col mt-6">
  <div class="flex flex-row py-8 border-t-2 border-transparent">
    <%= body %>
  </div>
</div>
