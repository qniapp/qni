<% body = yield.presence || raise('body is a mandatory') %>
<% klass = local_assigns[:class] %>

<%= tag.div class: class_string('flex flex-col', klass => klass), data: { target: 'simulator.qubitCircleGroup' } do %>
  <%= body %>
<% end %>
