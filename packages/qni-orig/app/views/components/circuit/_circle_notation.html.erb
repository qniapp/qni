<% body = yield.presence || raise('body is a mandatory') %>
<% size = local_assigns.fetch(:size, :base) %>

<%= tag.div class: class_string('circle-notation', 'circle-notation--small' => size == :small) do %>
  <%= body %>
<% end %>
