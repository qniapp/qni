<% body = yield.presence || raise('body is a mandatory') %>
<% nqubit = local_assigns.fetch(:nqubit) %>

<%= tag.div class: class_string('circle-notation', "circle-notation--#{nqubit}qubit"), data: { controller: 'circle-notation' } do %>
  <%= body %>
<% end %>
