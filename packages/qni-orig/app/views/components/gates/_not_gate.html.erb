<% top = local_assigns[:top] %>
<% bottom = local_assigns[:bottom] %>
<% label = local_assigns[:label] %>

<div class="not-gate">
  <%= bg_wire %>
  <%= top_wire if top %>
  <%= bottom_wire if bottom %>
  <%= gate_label(label: label) if label %>
  <%= gate_circle label: plus %>
</div>
