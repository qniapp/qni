<% label = local_assigns.fetch(:label, nil) %>
<% disabled = local_assigns.fetch(:disabled, false) %>
<% wire_active = local_assigns.fetch(:wire_active, true) %>

<%= tag.div class: class_string('gate', 'hadamard-gate', 'gate--disabled' => disabled, 'gate--inactive-wire' => !wire_active) do %>
  <%= gate_wire %>
  <div class="gate__box">H</div>
  <%= tag.div(label, class: 'gate__label') if label %>
<% end %>
