<% label = local_assigns.fetch(:label, nil) %>
<% disabled = local_assigns.fetch(:disabled, false) %>
<% wire_active = local_assigns.fetch(:wire_active, true) %>

<%= tag.div class: class_string('circuit-element', 'gate', 'hadamard-gate', 'gate--disabled' => disabled, 'circuit-element--inactive-wire' => !wire_active) do %>
  <%= circuit_element_wire %>
  <%= gate_box label: 'H' %>
  <%= circuit_element_label label: label %>
<% end %>
