<% rows = local_assigns.fetch(:rows) %>
<% body = yield.presence || raise('body is a mandatory') %>

<div class="grid gap-2 grid-cols-1 grid-rows-<%= rows %> mr-2 flex-shrink-0">
  <%= body %>
</div>
