<% label = local_assigns.fetch(:label) %>
<% row_span = local_assigns.fetch(:row_span) %>

<div class="register-label-span row-span-<%= row_span %>">
  <span class="register-label-span__label"><%= label %></span>
</div>
