<% body = yield.presence || raise('body is a mandatory') %>
<% klass = local_assigns.fetch(:class, nil) %>
<% data = local_assigns.fetch(:data, nil) %>

<%= tag.div class: class_string('circuit-card', klass => klass), data: data do %>
  <%= body %>
<% end %>
