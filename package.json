{
  "name": "@qni/monorepo",
  "private": true,
  "version": "0.0.1",
  "description": "Toy quantum simulator app.",
  "author": "@yasuhito",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/qniapp/qni.git"
  },
  "license": "MIT",
  "workspaces": [
    "packages/common",
    "packages/simulator",
    "packages/elements",
    "apps/www",
    "examples/elements-example"
  ],
  "scripts": {
    "build": "lerna run build --stream",
    "build:www": "yarn build:packages && cd apps/www && yarn build",
    "build:packages": "lerna run build:packages --stream",
    "start": "yarn build:packages && cd apps/www && ./bin/dev",
    "start:elements-example": "cd examples/elements-example && http-server",
    "test": "lerna run test --stream",
    "test:ci": "lerna run test:ci --stream",
    "publish:patch": "yarn build:packages && yarn test && lerna publish",
    "docs": "lerna run docs",
    "fix:style": "yarn run prettier ./packages/common/src --write && yarn run prettier ./packages/simulator/src --write && yarn run prettier ./packages/elements/src --write",
    "prepare": "husky install",
    "pretty-quick": "pretty-quick"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.8.1",
    "@typescript-eslint/parser": "^5.8.1",
    "eslint": "^8.5.0",
    "eslint-plugin-github": "^4.3.5",
    "husky": "^7.0.4",
    "lerna": "^4.0.0",
    "prettier": "^2.5.1",
    "pretty-quick": "^3.1.3"
  }
}
